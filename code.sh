# 1. install Node 18+ (nodejs.org or `apt install nodejs npm`)

# 2. create the project
mkdir czacute-server && cd czacute-server
#   ... create all the files above in that folder ...
mkdir library public src
#   (put index.html in public/, the .js files in src/)

# 3. install & start
npm install
npm start          # → http://localhost:3000
